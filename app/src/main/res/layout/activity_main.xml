<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/background"
    android:orientation="vertical"
    android:padding="@dimen/middle_mark">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start"
            android:text="@string/app_name"
            android:textColor="@color/white"
            android:textSize="@dimen/big_size_text"
            android:fontFamily="@font/ys_display_medium"/>
    </FrameLayout>

   <Button
       android:id="@+id/firstMainButton"
       style="@style/mainBtnLight"
       android:layout_marginTop="40dp"
       android:text="@string/search"
       app:icon="@drawable/main_activity_search"/>

    <Button
        android:id="@+id/secondMainButton"
        style="@style/mainBtnLight"
        android:text="@string/media_library"
        app:icon="@drawable/media_library"/>

    <Button
        android:id="@+id/thirdMainButton"
        style="@style/mainBtnLight"
        android:text="@string/settings"
        app:icon="@drawable/settings"/>
</LinearLayout>